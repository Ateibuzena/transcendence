docker compose down -v

docker compose up --build user-service user_db

curl -i http://localhost:5002/users 

docker ps --format "table {{.Names}}\t{{.Status}}"

docker exec -it user-service sh

docker exec -it user_db sh